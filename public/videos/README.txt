Drop your video file in this folder.

The video should be named: vibecoder.mp4

The code is configured to use this MP4 file. If you use a different filename, update the code in app/page.tsx to match your filename. 